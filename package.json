{
  "name": "compass-community",
  "version": "0.1.0",
  "private": true,
  "description": "The Compass Web3 community product prototype and launch foundation.",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build && npm run copy:static",
    "copy:static": "node scripts/copy-static.mjs",
    "preview": "vite preview --host 127.0.0.1"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^5.0.0",
    "vite": "^7.0.0"
  },
  "devDependencies": {}
}
